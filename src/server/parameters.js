var answerTime = 5; //answer time length in seconds
var submitTime = 5;
var minUserNo = 2;
var numLetters = 9;
var rounds = {
    letters: true,
    numbers: true
}


module.exports = {
    answerTime,
    minUserNo,
    submitTime,
    numLetters,
    rounds
};
